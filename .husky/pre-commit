#!/bin/sh
echo "🧪 Running tests before commit..."
npx jest test/ --no-coverage --forceExit
if [ $? -ne 0 ]; then
  echo "❌ Tests failed. Commit blocked."
  exit 1
fi
echo "✅ Tests passed. Proceeding with commit."
